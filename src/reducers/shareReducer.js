const defaultState = {
    shareLink:"",
    isShareVisible:false
}


export default function shareReducer(state = defaultState, action) {
    switch (action.type) {

        default:
            return state

    }
}


